import { StyleSheet } from 'react-native';
import * as CommonStyle from './index';

export default StyleSheet.create({
  rowContainer: {
    flex: 1,
    flexDirection: 'row',
    flexWrap: 'wrap',
    padding: 2
  }
})
